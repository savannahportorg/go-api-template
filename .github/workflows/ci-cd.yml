name: CI/CD Pipeline

on:
  workflow_call:
    inputs:
      service_name:
        description: 'Name of the service/image'
        required: true
        type: string
        default: 'csg-onboarding'
  push:
    branches: [ reqheaders ]
  pull_request:
    branches: [ reqheaders ]

jobs:
  build-and-push:
    name: Build and Push Docker image
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/dev'
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Get service name
        id: service
        run: |
          if [ "${{ github.event_name }}" == "workflow_call" ]; then
            echo "name=${{ inputs.service_name }}" >> $GITHUB_OUTPUT
          else
            echo "name=csg-onboarding" >> $GITHUB_OUTPUT
          fi

      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/${{ steps.service.outputs.name }}:beta
            ${{ secrets.DOCKERHUB_USERNAME }}/${{ steps.service.outputs.name }}:${{ github.sha }}
          cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/${{ steps.service.outputs.name }}:buildcache
          cache-to: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/${{ steps.service.outputs.name }}:buildcache,mode=max